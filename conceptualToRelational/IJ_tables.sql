/* Create the table for an I-||-----||-J relationship. */

-- Table I_J: Each row holds one entity of type I and one of type J.
CREATE TABLE i_j (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    x  CHAR(3),  -- example of other attributes of entity type I
    y  CHAR(2)   -- example of other attributes of entity type J
);
